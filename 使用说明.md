# 审计取证单生成器 使用说明

## 这是什么

一个本地小工具：填好表单，一键生成标准格式的《审计（调查）取证单》Word 文件，并自动整理附件。
无需联网，双击即用。

## 启动

- **Windows**：解压后双击 `审计取证单生成器.exe`
  - 首次运行如出现 Windows SmartScreen 蓝色提示 → 点「更多信息」→「仍要运行」（正常现象，软件没有数字签名）
- **Mac**：双击 `审计取证单生成器.app`
  - 如提示"无法打开"→ 系统设置 → 隐私与安全性 → 点「仍要打开」

## 界面说明

### 编号信息
| 字段 | 说明 |
|---|---|
| 取证单编号 | 数字即可，如 `01`、`02` |
| 审计项目类型 | 下拉选择：经责 / 财务收支 / 专项审计 / 工程审计 / 绩效审计 |
| 审计项目计划年度 | 4 位数字，如 `2026` |
| 被审计单位简称 | 不用手填，下方输入单位全称后自动带出 |
| 页眉编号预览 | 自动拼接的编号（SJ年度-类型-简称-编号），与 Word 页眉一致 |

### 项目信息
- **被审计（调查）单位**：输入关键字会自动弹出匹配列表，↑↓ 选择、回车确认，选中后简称自动填充
- **项目名称 / 审计（调查）事项**：直接填写
- **审计（调查）事项摘要**：多行文本；如果添加了附件，生成时会在摘要内容之后自动生成附件目录

### 附件（可选）
1. 把 PDF / Word / Excel 等文件**拖进虚线框**（或点击虚线框选择文件）
2. 按拖入顺序自动编号「附件1、附件2…」，名称自动取原文件名
3. 每个附件可以：**改名称**（直接点名称编辑）、**替换**（内容填错了换个文件重新选）、**↑↓ 调顺序**、**删除**
4. 生成后附件会和取证单一起放进输出文件夹，自动命名为「附件N-正式名称」

### 审计人员 / 编制日期
- 人员可填 1~2 人；日期默认今天，可改

### 底部按钮
- **生成取证单**：生成文件（见下）
- **暂存**：手动保存当前填写内容（平时每 60 秒也会自动暂存，不怕关窗口丢内容）
- **清空**：全部清空（含附件）
- **新增下一张**（生成过之后才出现）：编号自动 +1，只清空事项/摘要/附件，其余保留，方便连续出单

## 生成的文件在哪

程序所在目录下的 `output/` 文件夹。每张取证单是一个**独立文件夹**，例如：

```
output/
└── SJ2026-专项审计-凉山生态-02-存货管理/
    ├── SJ2026-专项审计-凉山生态-02-存货管理.docx   ← 取证单
    ├── 附件1-存货盘点表.pdf                        ← 附件（如有）
    └── 附件2-出入库明细.xlsx
```

生成成功后可点弹窗里的「打开输出文件夹」直接跳转。

## 常见问题

**Q：关掉窗口再打开，填的内容还在吗？**
在。每分钟自动暂存一次，也可以手动点「暂存」。注意：附件文件本体也会保留，但请不要删除程序目录下的 `.qzdd_attachments` 文件夹。

**Q：单位简称不对 / 新单位搜不到？**
简称来自程序内置的《公司机构排序简称简称发文代字表》，需要更新表格时联系维护人重新打包。

**Q：生成失败提示"附件文件已丢失"？**
说明暂存的附件文件被清理过，把该附件删除后重新拖入即可。

**Q：Mac 版提示应用已损坏？**
在终端执行 `xattr -cr 审计取证单生成器.app` 后再打开。
